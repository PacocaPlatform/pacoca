{
  "name": "pacoca-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:local": "wrangler d1 execute pacoca-levels --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute pacoca-levels --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.107.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20260101.0"
  }
}
